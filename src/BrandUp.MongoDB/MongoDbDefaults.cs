namespace BrandUp.MongoDB
{
    /// <summary>Default values used across the library.</summary>
    public static class MongoDbDefaults
    {
        /// <summary>Connection string pointing at a MongoDB instance on localhost, default port.</summary>
        public const string LocalConnectionString = "mongodb://localhost:27017";
    }
}